---
phase: 05-polish-and-regression-verification
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - SC-02
  - SC-03
  - SC-04
must_haves:
  truths:
    - "Non-MK8 configs (no grid.main) produce identical render paths to pre-MK8 behavior"
    - "Edge case configs (no battery, no solar, individuals, power outage) render without errors"
    - "Full test suite, typecheck, format check, and build all pass"
    - "ROADMAP Phase 3/4/5 statuses are up to date"
  artifacts:
    - path: "dist/power-flow-card-plus.js"
      provides: "Production build output"
  key_links:
    - from: "pnpm build"
      to: "dist/"
      via: "rollup bundler"
      pattern: "dist.*power-flow-card-plus"
---

<objective>
Verify edge case configurations, non-MK8 regression safety, run the full build pipeline, and update ROADMAP status.

Purpose: Confirm that all Phase 1-4 requirements remain intact after the dead code removal and SVG path changes. This is the final gate before the phase (and milestone) can be considered complete.
Output: Clean build, verified edge cases, updated ROADMAP.
</objective>

<execution_context>
@/home/groot/.claude/get-shit-done/workflows/execute-plan.md
@/home/groot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-regression-verification/05-RESEARCH.md
@.planning/phases/05-polish-and-regression-verification/05-01-SUMMARY.md
@.planning/phases/05-polish-and-regression-verification/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code-audit edge cases and non-MK8 regression, run full build</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Perform a systematic code audit of `src/power-flow-card-plus.ts` render() to verify correctness across all configuration modes. This is a READ-ONLY audit (no code changes) followed by a full build.

    **Non-MK8 Regression Audit (SC-02):**
    Trace the render path when `gridMain.has = false`:
    1. Verify `computeFlowGeometry(false, hasRightSection)` produces numCols=3 (or 4 with right section), matching pre-MK8 column structure
    2. Verify all `gridMain.has ? ... : ""` conditionals correctly emit nothing for non-MK8 (top row intermediate slots, Col B spacer, Col C spacer, bottom row intermediate/spacer)
    3. Verify the 3-col layout: Col A = nonFossil/grid, Col B(spacer with curves) = solar/battery, Col C = home/individuals
    4. Verify `leftReach`/`rightReach`/`straightLineLength` produce correct values for 3-col: gap = (360-240)/2 = 60, leftReach = -100, rightReach = 180, straightLineLength = 280. Same as pre-change values.
    5. Verify individual left-top/left-bottom flow lines (which are drawn inline in their circle-containers, NOT in spacers) are unchanged by Plan 02.

    **Edge Case Config Audit (SC-03):**
    Trace render paths for these configs:
    - **No battery** (`battery.has = false`): battery circle-container replaced by spacer. Battery-to-home, battery-grid, solar-to-battery SVGs gated by `battery.has &&` or `showLine(config, battery.state.toHome)`. Verify no uncaught access to `battery.state.*` when battery.has is false.
    - **No solar** (`solar.has = false`): solar circle-container replaced by spacer. Solar-to-home, solar-to-grid SVGs gated by `solar.has &&`. Solar-to-battery vertical line gated inside solar.ts. Verify no uncaught access to `solar.state.*`.
    - **With individual devices** (1, 2, 3, 4): Verify `hasRightSection` logic (true when >2 individuals). Verify right-individual flow SVGs use dynamic coordinates. Verify individual sort does not affect layout slot assignment.
    - **Power outage** (`grid.powerOutage.isOutage = true`): Verify grid element renders outage icon. Verify independent outage per meter (grid_house outage does not affect grid_main and vice versa).
    - **Full MK8** (grid.main + grid.house + intermediates + solar + battery + individuals): Verify all 5 or 6 columns render, all SVG paths use dynamic coordinates.
    - **Minimal** (grid + home only): Verify 3-col layout, no errors from absent solar/battery/individual.

    **Full Pipeline (SC-04):**
    Run the complete validation pipeline:
    ```
    pnpm typecheck && pnpm format:check && pnpm test && pnpm build
    ```

    **ROADMAP Cleanup:**
    Update `.planning/ROADMAP.md`:
    - Phase 3 status: change from `2/3 | In Progress` to `3/3 | Complete` (it IS functionally complete -- the roadmap was just not updated)
    - Phase 4 status: change from `0/2 | Not started` to `2/2 | Complete`
    - Phase 5 status: update with plan count from this planning session
    - Phase 3 Plans checkboxes: mark all `[x]`
    - Phase 4 Plans checkboxes: mark all `[x]`
    - Phase 5 Plans: update from "TBD" to actual plan names
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm format:check && pnpm test && pnpm build</automated>
  </verify>
  <done>
    - Code audit confirms non-MK8 3-col layout render path is unchanged
    - Code audit confirms no uncaught property access for no-battery, no-solar, power-outage configs
    - pnpm typecheck exits 0
    - pnpm format:check exits 0
    - pnpm test exits 0 (all 20 tests pass)
    - pnpm build exits 0 (dist/power-flow-card-plus.js produced)
    - ROADMAP.md Phase 3/4/5 statuses corrected
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of flow line alignment in Home Assistant</name>
  <action>
    User verifies the card visually in Home Assistant at multiple widths with MK8 and non-MK8 configs.
  </action>
  <verify>User confirms visual correctness or describes issues</verify>
  <done>User types "approved" confirming flow lines align correctly at all tested widths</done>
  <what-built>
    Complete Phase 5 polish: formatted codebase, removed dead flow overlay code (flowElement, flowStyle, .lines CSS, isCardWideEnough, all 9 empty flow component files), fixed SVG path alignment with dynamic coordinates based on actual card width, added flex-shrink protection to prevent circle compression, verified edge cases via code audit, full build pipeline green.
  </what-built>
  <how-to-verify>
    1. Load the card in Home Assistant with your MK8 config
    2. Check flow line alignment at the HA editor panel width (narrower than full dashboard)
    3. Verify gridMain-to-gridHouse line starts at gridMain center and ends at grid center
    4. Verify battery-to-grid line stays in the correct column
    5. Check the card at full dashboard width -- should look identical to before
    6. If you have a non-MK8 config, verify it still renders correctly (3-column layout)
    7. Type "approved" if lines align correctly, or describe any remaining issues
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm format:check && pnpm test && pnpm build` all exit 0
- dist/power-flow-card-plus.js exists and is a valid JS bundle
- ROADMAP.md reflects accurate phase completion statuses
- User visual verification confirms flow line alignment at multiple widths
</verification>

<success_criteria>
Full build pipeline green. Code audit confirms no regressions for non-MK8 users and edge case configs. ROADMAP reflects reality. User confirms visual alignment in Home Assistant.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-regression-verification/05-03-SUMMARY.md`
</output>
