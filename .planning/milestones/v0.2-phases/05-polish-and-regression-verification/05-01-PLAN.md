---
phase: 05-polish-and-regression-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/power-flow-card-plus.ts
  - src/utils/flowGeometry.ts
  - src/style.ts
  - src/style/all.ts
  - src/components/flows/index.ts
  - src/components/flows/solarToHome.ts
  - src/components/flows/solarToGrid.ts
  - src/components/flows/solarToBattery.ts
  - src/components/flows/gridToHome.ts
  - src/components/flows/gridMainToGridHouse.ts
  - src/components/flows/batteryToHome.ts
  - src/components/flows/batteryGrid.ts
  - src/components/flows/gridHouseToIntermediate.ts
  - src/components/flows/gridMainToIntermediate.ts
autonomous: true
requirements:
  - SC-04
must_haves:
  truths:
    - "pnpm format:check passes with zero warnings"
    - "No dead code remains from the old flow overlay system"
    - "pnpm typecheck and pnpm test still pass after cleanup"
  artifacts:
    - path: "src/utils/flowGeometry.ts"
      provides: "Only computeFlowGeometry and FlowGeometry interface remain"
      contains: "computeFlowGeometry"
    - path: "src/style.ts"
      provides: "CSS with .lines rules removed"
    - path: "src/power-flow-card-plus.ts"
      provides: "No flowElement import/call, no isCardWideEnough"
  key_links:
    - from: "src/power-flow-card-plus.ts"
      to: "computeFlowGeometry"
      via: "import from flowGeometry.ts"
      pattern: "import.*computeFlowGeometry"
---

<objective>
Format the codebase and remove all dead code from the old flow overlay system.

Purpose: Create a clean baseline before making substantive SVG path fixes. Running format:write first (per research Pitfall 3) ensures subsequent diffs are clean. Dead code removal eliminates confusion and reduces shipped CSS/JS.
Output: Formatted codebase with no dead flow overlay code.
</objective>

<execution_context>
@/home/groot/.claude/get-shit-done/workflows/execute-plan.md
@/home/groot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-regression-verification/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Prettier and commit formatting changes</name>
  <files>
    __tests__/migrate-config.test.ts
    src/components/flows/gridHouseToIntermediate.ts
    src/components/flows/gridMainToIntermediate.ts
    src/components/flows/index.ts
    src/components/gridMain.ts
    src/components/intermediate.ts
    src/logging.ts
    src/power-flow-card-plus.ts
    src/ui-editor/schema/_schema-all.ts
    src/ui-editor/ui-editor.ts
    src/utils/flowGeometry.ts
  </files>
  <action>
    Run `pnpm format:write` to auto-format all 11 files flagged by `pnpm format:check`.
    Commit the formatting changes SEPARATELY before any code changes, so subsequent diffs are clean.
    Do NOT make any code changes in this task -- purely formatting.
  </action>
  <verify>
    <automated>pnpm format:check</automated>
  </verify>
  <done>pnpm format:check exits 0 with no warnings</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead flow overlay code</name>
  <files>
    src/power-flow-card-plus.ts
    src/utils/flowGeometry.ts
    src/style.ts
    src/style/all.ts
    src/components/flows/index.ts
    src/components/flows/solarToHome.ts
    src/components/flows/solarToGrid.ts
    src/components/flows/solarToBattery.ts
    src/components/flows/gridToHome.ts
    src/components/flows/gridMainToGridHouse.ts
    src/components/flows/batteryToHome.ts
    src/components/flows/batteryGrid.ts
    src/components/flows/gridHouseToIntermediate.ts
    src/components/flows/gridMainToIntermediate.ts
  </files>
  <action>
    Remove all dead code from the old flow overlay system. Specifically:

    1. **src/utils/flowGeometry.ts**: Delete `flowStyle()`, `flowStyleVertical()`, and `flowPixelWidth()` functions. Keep ONLY the `FlowGeometry` interface and `computeFlowGeometry()` function -- these are still used.

    2. **src/style.ts**: Delete the `.lines` CSS block (lines 87-96: `.lines { position:absolute; ... }` and `.lines svg { ... }`). These rules have no consumers since all flows are inline SVGs now.

    3. **src/power-flow-card-plus.ts**:
       - Delete the `isCardWideEnough` variable (line 642: `const isCardWideEnough = this._width > 420;`).
       - Remove the `isCardWideEnough` property from the `allDynamicStyles()` call (line 654). The `allDynamicStyles` function accepts `[key: string]: any` but never reads this value (confirmed: grep of all.ts shows zero references).
       - Delete the `import { flowElement } from "./components/flows";` import (line 7).
       - Delete the `flowElement(this._config, { ... })` call and its entire template block (around line 1000-1009). All 9 child flow functions return empty strings, so this call produces an empty lit `html` template with zero visual output.

    4. **Delete all 9 flow component files** in `src/components/flows/`:
       - `solarToHome.ts`, `solarToGrid.ts`, `solarToBattery.ts`, `gridToHome.ts`, `gridMainToGridHouse.ts`, `batteryToHome.ts`, `batteryGrid.ts`, `gridHouseToIntermediate.ts`, `gridMainToIntermediate.ts`
       Every one of these returns `""`. They are dead code.

    5. **Delete `src/components/flows/index.ts`**: The `flowElement()` orchestrator and `Flows` interface. Since the flowElement call is removed from power-flow-card-plus.ts and all child files are deleted, this file has no consumers. The `FlowGeometry` type is imported directly from `flowGeometry.ts` where needed.

    After deletions, verify no remaining imports reference the deleted files:
    - `grep -r "from.*flows" src/` should return nothing (or only unrelated imports)
    - `grep -r "flowElement" src/` should return nothing
    - `grep -r "flowStyle\|flowPixelWidth\|isCardWideEnough" src/` should return nothing
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test</automated>
  </verify>
  <done>
    - flowStyle, flowStyleVertical, flowPixelWidth functions deleted from flowGeometry.ts
    - .lines CSS rules deleted from style.ts
    - isCardWideEnough variable and its propagation deleted from power-flow-card-plus.ts
    - flowElement import and call deleted from power-flow-card-plus.ts
    - All 9 flow component files and flows/index.ts deleted
    - pnpm typecheck passes (no broken imports)
    - pnpm test passes (no regressions)
  </done>
</task>

</tasks>

<verification>
- `pnpm format:check` exits 0
- `pnpm typecheck` exits 0
- `pnpm test` exits 0 with all 20 tests passing
- `grep -r "flowElement\|flowStyle\|flowPixelWidth\|isCardWideEnough" src/` returns nothing
- `ls src/components/flows/` returns empty or directory does not exist
</verification>

<success_criteria>
All dead flow overlay code is removed. Formatting is clean. Type checking and tests pass. The codebase is ready for SVG path fixes in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-regression-verification/05-01-SUMMARY.md`
</output>
